const colors = {
    primary: '#7cccb1',      // Button, highlights
    secondary: '#27386c',    // Titles, main text
    accent: '#6fa0b1',       // Subtle text
    background: '#ffffff',   // Background color
    backgroundMain: '#f3f6f9',
    textLight: '#fff',       // Light text, for buttons
    border: '#ccc',     
    placeholder: '#7f8c8d',      
  };
  
  const fonts = {
    small: 14,
    medium: 16,
    large: 18,
    xlarge: 24,
  };
  
  const spacing = {
    small: 8,
    medium: 16,
    large: 24,
  };
  
  const theme = {
    colors,
    fonts,
    spacing,
  };
  
  export default theme;
  